/*
 * Complete the function below.
 */
string lexicographically_smallest(vector < string > substrings) {
	int all_l = 0;
	for (int i = 0; i < substrings.size(); ++i)
	{
		all_l += substrings[i].length();
	}
	vector<vector<int> > dp;
	dp.resize() 

}

