#include <iostream>
#include <vector>

template<typename K>
struct ListNode {
	K value;
	ListNode<K>* next;
	ListNode<
};


template <typename K, typename V>
class Hash
{
private:
	vector<ListNode<T>* > rolling_hash;
	hash_size;
public:
	Hash(){
		hash_size = 10000;
		rolling_hash.resize(hash_size);
	}


};

