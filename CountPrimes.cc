class Solution {
public:
    int countPrimes(int n) {
    	vector<int> hurui = vector<int>
    	int start = 1;
    	int root =　sqrt(n); 
    	while(start <= root){
    		if(n % start == 0)return false;
    	}
        
    }
};