class Solution {
public:
    vector<int> lexicalOrder(int n) {
    	vector<int> ans;
    	if(n == 0)retrun ans;
    	ans.push_back(1);
    	int cur = 1;
    	while(tmp <= n){
    		tmp = tmp*10;
    		


    	}

    	for (int i = 0; i < n; ++i)
    	{
    		
    	}

    	return ans;
        
    }
};